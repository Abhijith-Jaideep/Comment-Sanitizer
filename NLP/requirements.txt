# Pinned because the two pickles are scikit-learn objects: unpickling under a
# different major version is not guaranteed to work, and when it does fail it
# fails at import time in NLP.py rather than at request time. Train and serve
# on the same versions.
flask==3.1.3
pandas==3.0.5
scikit-learn==1.9.0
scipy==1.18.0
numpy==2.5.1
