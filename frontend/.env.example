# Copy to .env.local and fill in.
#
# Create React App inlines REACT_APP_* at BUILD time, not runtime. Changing
# this after a build has no effect; you have to rebuild.

# Optional. Defaults to http://localhost:5000
# In production set this to the deployed API URL, no trailing slash.
REACT_APP_API_URL=
